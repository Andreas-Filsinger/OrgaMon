SELECT 
  ARTIKEL.RID,
  ARTIKEL.NUMERO,
  ARTIKEL.TITEL,
  PRORATA.PERCENT,
  ARTIKEL.VERLAG_STAT_START,
  0 AUSGABEART_R
FROM 
 PRORATA
JOIN 
 ARTIKEL 
ON
 (PRORATA.ARTIKEL_R=ARTIKEL.RID)
WHERE
 PRORATA.PERSON_R=:CROSSREF
ORDER BY 
 ARTIKEL.TITEL
FOR UPDATE
